<?xml version="1.0" encoding="utf-8"?>
<!--
  Buttons, a way back, and the version. No prose.
  Built like the sliders: each control takes an equal share of the height, so the whole screen
  is a target rather than a list with dead space under it.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/settings_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ground"
    android:orientation="vertical"
    android:paddingStart="20dp"
    android:paddingEnd="20dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageView
            android:id="@+id/back_button"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:contentDescription="Back"
            android:padding="10dp"
            android:src="@drawable/ic_back" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1" />

        <TextView
            android:id="@+id/version"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/slate_ink"
            android:textSize="16sp" />
    </LinearLayout>

    <TextView
        android:id="@+id/banner_button"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="12dp"
        android:background="@color/slate"
        android:gravity="center"
        android:textColor="@color/sand"
        android:textSize="20sp" />

    <TextView
        android:id="@+id/dnd_button"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="12dp"
        android:background="@color/slate"
        android:gravity="center"
        android:textColor="@color/sand"
        android:textSize="20sp" />

    <TextView
        android:id="@+id/copy_button"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="12dp"
        android:background="@color/slate"
        android:gravity="center"
        android:text="Copy the report"
        android:textColor="@color/sand"
        android:textSize="20sp" />
</LinearLayout>
