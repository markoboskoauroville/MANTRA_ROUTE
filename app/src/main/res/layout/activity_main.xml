<?xml version="1.0" encoding="utf-8"?>
<!--
  Sliders, and one way out. Nothing else.

  Every title, caption and credit was removed: they were read once and then occupied the screen
  for ever, pushing the only controls that matter into a third of it. The rows now share the
  full height, so each is as tall as the phone allows.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root_scroll"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/ground"
    android:orientation="vertical"
    android:paddingStart="20dp"
    android:paddingEnd="20dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <ImageView
        android:id="@+id/settings_button"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="end"
        android:contentDescription="Settings"
        android:padding="10dp"
        android:src="@drawable/ic_settings" />

    <LinearLayout
        android:id="@+id/volume_rows"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical" />
</LinearLayout>
