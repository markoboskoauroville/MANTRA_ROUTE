<?xml version="1.0" encoding="utf-8"?>
<!--
  Volume in the shade. A notification cannot host a SeekBar — RemoteViews only permits a fixed
  set of widgets and a slider is not one of them. So the row is stepped: minus, the reading,
  plus. Three targets, each stating what the next press does. §5.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="6dp"
    android:paddingBottom="6dp">

    <ImageView
        android:id="@+id/vol_glyph"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginStart="10dp"
        android:contentDescription="@null" />

    <TextView
        android:id="@+id/vol_label"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="10dp"
        android:maxLines="1"
        android:textSize="14sp" />

    <TextView
        android:id="@+id/vol_down"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:text="−"
        android:textSize="18sp" />

    <TextView
        android:id="@+id/vol_up"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="14dp"
        android:paddingEnd="14dp"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:text="+"
        android:textSize="18sp" />
</LinearLayout>
